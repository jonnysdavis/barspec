{
  "name": "barspec",
  "version": "0.1.0",
  "description": "Desktop app for controlling Beyond All Reason spectator camera with a gamepad",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "npm run build -- --watch"
  },
  "dependencies": {
    "electron": "^30.0.0",
    "robotjs": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/robotjs": "^0.6.0",
    "typescript": "^5.4.5"
  }
}
